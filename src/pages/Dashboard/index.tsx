import { Box, Heading } from "@chakra-ui/react"
import { Header } from "../../components/Header"

export const Dashboard = () => {
    return(
        <Box>
            <Header/>
        </Box>
    )
}